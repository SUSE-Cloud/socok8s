exclude_paths:
  - submodules
  - playbooks/roles/describe-kubernetes-objects
  - playbooks/roles/gather-pod-logs
  - playbooks/roles/helm-release-status
  - playbooks/roles/ses
# Allow warning 204, which means we are enabling longer than 120 chars lines.
skip_list:
  - '204'
parseable: true
